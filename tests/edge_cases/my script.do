* Test script with spaces in filename
* This tests that stacy handles filenames with spaces correctly

display "Testing spaces in filename"
display "Script name: my script.do"

* Simple successful execution
summarize
